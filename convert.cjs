const fs = require('fs'); const content = fs.readFileSync('new-premium-logo.html', 'utf8'); let svgHtml = content.match(/<svg[^>]*>([\s\S]*?)<\/svg>/)[0]; svgHtml = svgHtml.replace(/<svg[^>]*>/, '<svg className={className} viewBox=\"0 0 1024 1024\" preserveAspectRatio=\"xMidYMid meet\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\">').replace(/stop-color/g, 'stopColor').replace(/stop-opacity/g, 'stopOpacity').replace(/stroke-width/g, 'strokeWidth'); fs.writeFileSync('src/components/AnimatedLogo.jsx', 'import React from \'react\';\nimport { motion } from \'framer-motion\';\n\nconst AnimatedLogo = ({ className }) => (\n' + svgHtml + '\n);\n\nexport default AnimatedLogo;');
